import React from 'react';
import Header from '../components/Header';

const Departments = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Departments;